# Server Configuration
PORT=5000
NODE_ENV=production

# Database Configuration (RDS / Production DB)
DB_HOST=your-rds-endpoint.amazonaws.com
DB_USER=admin
DB_PASS=securepassword
DB_NAME=razia_ecommerce
DB_PORT=3306

# Security
JWT_SECRET=paste_high_entropy_secret_string_here_minimum_64_chars

# Payment Gateway (Paymob)
PAYMOB_API_KEY=your_production_api_key
PAYMOB_HMAC_SECRET=your_production_hmac_secret
PAYMOB_INTEGRATION_ID=your_integration_id
PAYMOB_IFRAME_ID=your_iframe_id

# Email Service (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_specific_password

# Uploads
# If using S3 for production, add S3 config here. 
# Otherwise ensures 'uploads/' directory is persistent volume in Docker.
